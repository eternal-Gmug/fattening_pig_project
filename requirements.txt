# 项目依赖库 - 必需
opencv-python>=4.5.0
numpy>=1.20.0
onnxruntime>=1.9.0
PySide6>=6.0.0
ultralytics>=8.0.0  # YOLOv8库，从test.py中发现使用

# 开发和打包依赖
pyinstaller>=5.0.0  # 用于打包应用程序

# 可选依赖（用于扩展功能）
# torch>=1.9.0  # 如果需要模型训练或微调
# torchvision>=0.10.0  # PyTorch的计算机视觉库
# matplotlib>=3.4.0  # 如果需要可视化功能
# pillow>=8.0.0  # 图像处理增强